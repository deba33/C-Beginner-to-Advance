#include <stdio.h>

int main()
{
    int number = 25;
    int my_age = 22; // int: integer data type (both +ve and -ve)
    // value of Declared variable can be changed 
    my_age = 34; 
    float _pi = 3.14; // float: Decimal values (both +ve and -ve)
    char star = '*';
    char NAME = 'X#AIGD'; // char: all characters (strings)
    return 0;
}